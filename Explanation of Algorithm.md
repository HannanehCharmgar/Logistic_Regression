#  رگرسیون لجستیک (Logistic Regression)

## 🔹رگرسیون لجستیک چیست؟

- یک **الگوریتم نظارت‌شده** برای پیش‌بینی متغیر **گسسته** (معمولاً دودویی: `0` یا `1`).
- خروجی: **احتمال تعلّق به کلاس 1**، در بازه $[0, 1]$.
- نام "رگرسیون" به دلیل استفاده از یک **ترکیب خطی** از ویژگی‌هاست — نه به خاطر پیش‌بینی عدد پیوسته.

### مثال‌های کاربردی:
| حوزه | مسئله | کلاس 1 |
|------|--------|--------|
| پزشکی | تشخیص بیماری | بیمار = 1 |
| ایمیل | تشخیص اسپم | اسپم = 1 |
| بازاریابی | خرید محصول | خرید = 1 |

---

## تفاوت با رگرسیون خطی

| ویژگی | رگرسیون خطی | رگرسیون لجستیک |
|--------|-------------|----------------|
| خروجی | عدد پیوسته ($\mathbb{R}$) | احتمال ($[0,1]$) |
| کاربرد | پیش‌بینی قیمت، دما، ... | دسته‌بندی (بله/خیر) |
| تابع پیش‌بینی | $\hat{y} = \mathbf{w}^T \mathbf{x}$ | $\hat{p} = \sigma(\mathbf{w}^T \mathbf{x})$ |
| تابع هزینه | MSE | Cross-Entropy (Log Loss) |
>  اگر از رگرسیون خطی برای دسته‌بندی استفاده کنیم:  
> - خروجی ممکن است $<0$ یا $>1$ باشد → **بی‌معنی** برای احتمال.  
> - تابع هزینه غیرمحدب → بهینه‌سازی سخت.

---

## 🔹تابع سیگموئید (Sigmoid)

قلب تپنده رگرسیون لجستیک:

$$
\sigma(z) = \frac{1}{1 + e^{-z}}
$$

### ویژگی‌ها:
- $\sigma(0) = 0.5$
- $\displaystyle \lim_{z \to +\infty} \sigma(z) = 1$
- $\displaystyle \lim_{z \to -\infty} \sigma(z) = 0$
- مشتق: $\sigma'(z) = \sigma(z)(1 - \sigma(z))$

### نمودار سیگموئید (توصیفی):

<img width="555" height="390" alt="image" src="https://github.com/user-attachments/assets/7c842ee0-75f3-4c89-ad27-ec3006c40ec6" />


> - محور افقی: $z = \mathbf{w}^T\mathbf{x}$  
> - محور عمودی: $\hat{p} = P(y=1 \mid \mathbf{x})$

---

## 🔹 مدل ریاضی کامل

برای یک نمونه $\mathbf{x} = [1, x_1, x_2, ..., x_n]$:

**ترکیب خطی**: 

  <img width="257" height="33" alt="image" src="https://github.com/user-attachments/assets/ffccc9d2-7dba-4b7d-8fe8-e8fd134decc3" />


**تبدیل به احتمال(اعمال سیگموئید)**:

<img width="254" height="40" alt="image" src="https://github.com/user-attachments/assets/db131b9c-79db-41ff-90db-0fe61c358c92" />


  
**تصمیم‌گیری نهایی** (در inference):  

  <img width="151" height="57" alt="image" src="https://github.com/user-attachments/assets/67a97fdd-3946-4d40-a631-0a42ca0bee92" />
  
آستانه 0.5 قابل تنظیم است(مثلا در موضوعات حساس پزشکی به 0.3 یا 0.7 تغییر می کند.)
  
  ---

## 🔹  تابع هزینه: Log Loss (Cross-Entropy)

برای یک نمونه با برچسب واقعی $y \in \{0, 1\}$:

$$
\mathcal{L}(y, \hat{p}) = - \Big[ y \log(\hat{p}) + (1 - y) \log(1 - \hat{p}) \Big]
$$

###  چرا این تابع؟
| شرایط | هزینه | تفسیر |
|--------|--------|--------|
| $y=1, \hat{p} \to 0$ | $\to +\infty$ | پیش‌بینی غلط بسیار بد! |
| $y=0, \hat{p} \to 1$ | $\to +\infty$ | پیش‌بینی غلط بسیار بد! |
| $y=1, \hat{p}=1$ | $0$ | کامل |
| $y=0, \hat{p}=0$ | $0$ | کامل |
 
+.
برای m نمونه:

<img width="374" height="50" alt="image" src="https://github.com/user-attachments/assets/145b8617-9765-4670-a8f3-93ec49a2417c" />



این تابع **محدب** است: گرادیان کاهشی به جواب **بهینه سرتاسری** می‌رسد.

---

## 🔹 بهینه‌سازی: گرادیان کاهشی

گرادیان تابع هزینه نسبت به $w_j$:

$$
\frac{\partial J}{\partial w_j} = \frac{1}{m} \sum_{i=1}^{m} \big( \hat{p}^{(i)} - y^{(i)} \big) x_j^{(i)}
$$

فرمول بروزرسانی:

$$
w_j := w_j - \alpha \cdot \frac{1}{m} \sum_{i=1}^{m} \big( \hat{p}^{(i)} - y^{(i)} \big) x_j^{(i)}
$$

## 🔹 تفسیر ضرایب: Odds و Log-Odds

### تعاریف:
- **Odds** (شانس):
  
 <img width="192" height="50" alt="image" src="https://github.com/user-attachments/assets/783ed466-b5d2-4f9a-b7b5-238514e54e39" />


- **Log-Odds** (یا Logit):  

 <img width="269" height="42" alt="image" src="https://github.com/user-attachments/assets/66f94025-7eb8-4ff3-8f74-af433039c711" />
 


پس مدل **مستقیماً روی log-odds خطی است**.

###  تفسیر ضریب $w_j$:
- افزایش ۱ واحدی در $x_j$ → log-odds به‌اندازه $w_j$ افزایش می‌یابد.
- یا: **Odds** در $e^{w_j}$ برابر ضرب می‌شود.

#### مثال:
- $w_j = 0.05$ → Odds جدید = $e^{0.05} \approx 1.051$ → **%5.1 افزایش شانس**  
- $w_j = -1.2$ → Odds جدید = $e^{-1.2} \approx 0.30$ → **%70 کاهش شانس**

| علامت $w_j$ | تأثیر روی کلاس 1 |
|-------------|------------------|
| $w_j > 0$ | افزایش شانس |
| $w_j < 0$ | کاهش شانس |
| $w_j = 0$ | تأثیری ندارد |

---

## 🔹مثال عددی: پیش‌بینی قبولی در امتحان

###  داده‌ها:
| نمونه | $x$ (ساعت مطالعه) | $y$ (نتیجه) |
|-------|-------------------|-------------|
| 1     | 1                 | 0           |
| 2     | 2                 | 0           |
| 3     | 3                 | 1           |
| 4     | 4                 | 1           |

مدل: $z = w_0 + w_1 x$, $\hat{p} = \sigma(z)$

### ✅ مرحله اولیه ($w_0 = 0$, $w_1 = 0$):
- $z = 0$ → $\hat{p} = 0.5$ برای همه  
- Log Loss هر نمونه: $-\log(0.5) = 0.693$  
- $J = 0.693$

### یک دور گرادیان کاهشی ($\alpha = 0.1$):
- $\frac{\partial J}{\partial w_0} = \frac{1}{4}[(0.5-0)+(0.5-0)+(0.5-1)+(0.5-1)] = 0$
- $\frac{\partial J}{\partial w_1} = \frac{1}{4}[0.5·1 + 0.5·2 + (-0.5)·3 + (-0.5)·4] = -0.5$
- بروزرسانی:
  - $w_0 := 0 - 0.1·0 = 0$
  - $w_1 := 0 - 0.1·(-0.5) = 0.05$

### پس از چند دور:
- $w_0 \approx -3.0$, $w_1 \approx 1.5$
- برای $x = 2$: $z = 0$ → $\hat{p} = 0.5$  
→ **مرز تصمیم**: ~۲ ساعت مطالعه

---


##  مزایا و محدودیت‌ها

### ✅ مزایا:
- ساده و سریع در آموزش و پیش‌بینی  
- خروجی به‌صورت **احتمال** → تصمیم‌گیری هوشمند  
- **قابل تفسیر**: ضرایب → تأثیر ویژگی‌ها  
- پایه‌ای برای مدل‌های پیچیده‌تر (مثل شبکه‌های عصبی)

### ❌ محدودیت‌ها:
- فقط برای روابط **تقریباً خطی در فضای log-odds** مناسب است  
- برای داده‌های غیرخطی (مثل XOR) شکست می‌خورد  
- در حضور **هم‌خطی شدید** (multicollinearity)، تفسیر ضرایب سخت می‌شود

### 🛠 راهکارهای بهبود:
- مهندسی ویژگی (مثلاً $x^2, \log(x), x_1x_2$)  
- استانداردسازی ویژگی‌ها (`StandardScaler`)  
- استفاده از **تنظیم‌کننده** (Regularization):
  - `penalty='l2'`: جلوگیری از بیش‌برازش  
  - `penalty='l1'`: انتخاب ویژگی (sparse model)

---

# جمع بندی تصویری

```
رگرسیون لجستیک
│
├── ورودی: ویژگی‌های پیوسته/گسسته (x₁, x₂, ...)
├── مرحله ۱: z = w₀ + w₁x₁ + ... + wₙxₙ  (ترکیب خطی)
├── مرحله ۲: p = σ(z) = 1/(1+e⁻ᶻ)       (سیگموئید → احتمال)
├── مرحله ۳: ŷ = 1 if p ≥ τ else 0      (تصمیم‌گیری)
│
├── آموزش:
│   ├── تابع هزینه: Log Loss
│   └── بهینه‌سازی: گرادیان کاهشی
│
├── تفسیر:
│   ├── wⱼ > 0 → افزایش شانس کلاس 1
│   └── e^{wⱼ} = نسبت odds جدید به قدیم
│
└── کاربرد: دسته‌بندی دودویی (اسپم، بیماری، خرید، ...)
```

---


 **نکته**:  
رگرسیون لجستیک فقط یک "الگوریتم" نیست — یک **چارچوب مدل‌سازی احتمالی** است که به شما کمک می‌کند بپرسید:  
 **"چه عواملی احتمال رخداد یک پیشامد را افزایش/کاهش می‌دهند، و چقدر؟"**

--- 
